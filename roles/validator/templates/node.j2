[parity]
chain = "{{ home }}/spec.json"

[rpc]
disable = false
interface = "0.0.0.0"
apis = ["web3", "eth", "parity", "parity_set", "personal", "net", "traces", "rpc"]
processing_threads = {{ processor_count }}
cors=["all"]

[websockets]
disable = false
interface = "0.0.0.0"
hosts = ["all"]
origins = ["all"]
apis = ["web3", "eth", "parity", "parity_set", "personal", "net", "traces", "rpc"]

[ipc]
disable = false
apis = ["web3", "eth", "parity", "parity_set", "personal", "net", "traces", "rpc"]
path = "/tmp/parity.ipc"

[ui]
disable = true

[network]
reserved_peers = "{{ home }}/bootnodes.txt"
discovery = false

[account]
password = ["{{ home }}/node.pwd"]

[mining]
usd_per_tx = "0"
engine_signer = "0x{{ account.stdout }}"
min_gas_price = 1000000000
gas_floor_target = "670000000"
tx_queue_size = 65536
force_sealing = true
reseal_on_txs = "none"
notify_work = ["http://localhost:8080/workpackage"]

[dapps]
disable = true

[misc]
log_file = "{{ home }}/logs/parity.log"
# Logging pattern (`<module>=<level>`, e.g. `own_tx=trace`).
logging = "miner=info"